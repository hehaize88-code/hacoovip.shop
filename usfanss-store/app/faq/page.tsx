import { SiteShell } from "../site-shell";
import type { Metadata } from "next";
export const metadata: Metadata = { title:"USFans FAQ 2026 | QC Photos, Returns and Shipping", description:"Answers to common USFans questions about warehouse photos, the five-day return request window, seller delivery and international shipping." };
export default function FaqPage() { return <SiteShell page="faq" />; }
