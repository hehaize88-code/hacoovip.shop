import { SectionPage } from "../site"; import { makeMetadata } from "../seo"; export const metadata=makeMetadata("finds","en","Superbuy Finds 2026: Unique Product Links & Prices","Browse distinct Superbuy product finds with matching images, approximate USD prices and direct links to each product detail page."); export default function Page(){return <SectionPage locale="en" section="finds"/>}
