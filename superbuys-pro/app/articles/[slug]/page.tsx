import { notFound } from "next/navigation"; import { ArticlePage } from "../../site"; import { articles } from "../../content"; import { makeMetadata } from "../../seo"; export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const article=articles.find(a=>a.slug===slug);if(!article)return {};return makeMetadata(`articles/${slug}`,"en",article.title.en,article.description.en)} export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;if(!articles.some(a=>a.slug===slug))notFound();return <ArticlePage locale="en" slug={slug}/>}
