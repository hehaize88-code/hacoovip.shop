# hacoovip.shop 部署说明

这个仓库已经是单独站点仓库，不再使用 `seo-sites/hacoovip-shop` 子目录。

Cloudflare Pages 设置：

- Repository：`hehaize88-code/hacoovip.shop`
- Production branch：`main`
- Framework preset：`None`
- Build command：留空
- Build output directory：`/`
- Custom domain：`hacoovip.shop`

保存后 Cloudflare 会自动部署。部署完成后先打开 `*.pages.dev` 预览，再绑定 `hacoovip.shop`。
